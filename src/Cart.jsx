import { useState } from 'react'
import EventData from "./EventData.json"

function Cart(selectedEvents, setSelectedEvents, cartView, setCartView) {
    if (cartView == true) {
        console.log('cart view successful!')
    }
}
export default Cart